import 'package:flutter/material.dart';

const List<BoxShadow> primaryShadows = [
  BoxShadow(
      color: Color.fromARGB(255, 44, 43, 43),
      blurRadius: 3,
      offset: Offset(3, 3))
];
List<BoxShadow> containerShadows = [
  BoxShadow(color: Colors.black.withOpacity(0.3), blurRadius: 2),
  BoxShadow(color: Colors.white.withOpacity(0.3), blurRadius: 2)
];
List<BoxShadow> itemsShadows = [
  BoxShadow(color: Colors.black.withOpacity(0.5), blurRadius: 2),
  BoxShadow(color: Colors.white.withOpacity(0.5), blurRadius: 2)
];
const List<BoxShadow> tileShadows = [
  BoxShadow(
      color: Color.fromARGB(255, 44, 43, 43),
      blurRadius: 3,
      offset: Offset(3, 3))
];
